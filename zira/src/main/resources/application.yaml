server:
  port: 8000
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/zira
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
